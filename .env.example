# API keys for cluster-whisperer demo cluster setup
# Copy to .env and fill in your values:
#   cp .env.example .env

# Required for capability inference pipeline and cluster-whisperer serve
ANTHROPIC_API_KEY=
VOYAGE_API_KEY=

# Required for OTel Collector → Datadog exporter (optional — skipped if unset)
DD_API_KEY=
